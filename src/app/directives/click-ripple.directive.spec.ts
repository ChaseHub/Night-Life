import { ClickRippleDirective } from './click-ripple.directive';

describe('ClickRippleDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickRippleDirective();
    expect(directive).toBeTruthy();
  });
});
